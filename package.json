{
  "name": "iva-bitrix24",
  "version": "0.4.1",
  "private": true,
  "description": "Bitrix24 integration plugin for Iva with bounded task, discussion, project, people and attachment reading.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mamysh/iva-bitrix24.git"
  },
  "bugs": {
    "url": "https://github.com/mamysh/iva-bitrix24/issues"
  },
  "homepage": "https://github.com/mamysh/iva-bitrix24#readme",
  "keywords": [
    "iva",
    "bitrix24",
    "mcp",
    "agent-plugin",
    "tasks"
  ],
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:bundle": "node scripts/check-bundle.mjs",
    "check:distribution": "node scripts/check-distribution.mjs",
    "check:secrets": "node scripts/check-secrets.mjs",
    "smoke:live": "node scripts/smoke-live.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test server/test/*.test.ts",
    "check": "npm run check:secrets && npm run check:distribution && npm run typecheck && npm test && npm run build && npm run check:bundle"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@types/node": "26.4.1",
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "zod": "4.6.1"
  },
  "engines": {
    "node": ">=24"
  }
}
